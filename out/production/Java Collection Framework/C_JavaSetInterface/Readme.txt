Set interface of the java collection framework provides
the features of the mathematical set in java.

It extends the collection interface.

Unlike the list interface,
set cannot contain duplicate elements.

Set -> EnumSet, G=HashSet, LinkedHashSet, TreeSet