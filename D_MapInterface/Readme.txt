In java elements of map  are stored in key/value
pairs keys are unique value associated with individual
values.

A map cannot contain duplicate keys and each key
associated woth single value

Map
|-HashMap        TC gets O(1) in some operations
|-TreeMap        Tc gets Olog(n) in some operations
|-EnumMap
|-LinkedHashMap  TC gets O(n) in some operations
|-WeakHashMap